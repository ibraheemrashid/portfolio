Drop screenshots for this project here, named 1.png, 2.png, 3.png (matching
the paths already listed in src/app/data/projects.data.ts).

Until real images exist, the <app-shot> component shows a clean placeholder
automatically — nothing breaks if this folder stays empty.
