Add your profile photo here, named profile.jpg (matching the path
referenced in src/app/pages/home/home.component.html).

Until it exists, the hero's photo slot shows a clean "Add your photo
here" placeholder automatically — nothing breaks if this stays empty.

Recommended: a portrait-oriented photo (roughly 4:5), reasonably high
resolution — it'll be displayed at up to ~360px wide with rounded
corners and a soft glow behind it.
